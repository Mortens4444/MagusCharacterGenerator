<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MAGUS.Assistant.ViewModels"
             xmlns:mc="clr-namespace:Mtf.Maui.Controls;assembly=Mtf.Maui.Controls"
             xmlns:mv="clr-namespace:MAGUS.Assistant.Views"
             xmlns:local="clr-namespace:MAGUS.GameSystem.Qualifications;assembly=MAGUS"
             xmlns:q="clr-namespace:MAGUS.GameSystem.Qualifications;assembly=MAGUS"
             xmlns:conv="clr-namespace:Mtf.LanguageService.MAUI.Converters;assembly=Mtf.LanguageService.MAUI"
             xmlns:converters="clr-namespace:MAGUS.Assistant.Converters"
             xmlns:mcc="clr-namespace:Mtf.Maui.Controls.Converters;assembly=Mtf.Maui.Controls"
             x:Class="MAGUS.Assistant.Views.QualificationsView"
             x:ClassModifier="internal"
             x:DataType="vm:CharacterViewModel">

    <ContentView.Resources>
        <ResourceDictionary>
            <conv:TranslationConverter x:Key="TranslationConverter" />
            <converters:QualificationNameConverter x:Key="QualificationNameConverter" />
            <converters:QualificationSorter x:Key="QualificationSorter" />
            <converters:SpecialQualificationNameConverter x:Key="SpecialQualificationNameConverter" />
            <converters:QualificationChooseButtonVisibleConverter x:Key="QualificationChooseButtonVisibleConverter" />
            <converters:LiteralTranslationConverter x:Key="LiteralTranslationConverter" />
            <mcc:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        </ResourceDictionary>
    </ContentView.Resources>
    
    <ScrollView>
        <VerticalStackLayout Spacing="16" Padding="16">

            <!-- Qualifications -->
            <Border Style="{StaticResource BoxBorder}" Margin="0,10">
                <VerticalStackLayout>
                    <Grid ColumnDefinitions="*,*">
                        <mc:EntryWithLabel Label="SP" Text="{Binding QualificationPoints}" IsReadOnly="True" />
                        <mc:MenuItem ImageName="qualifications" LabelText="Qualifications" PageType="{x:Type mv:QualificationsPage}" WidthRequest="100" Grid.Column="1"
                                 Parameter="{Binding Character}" IsVisible="{Binding CanAllocateQualificationPoints, Converter={StaticResource BoolToVisibilityConverter}}" />
                    </Grid>

                    <Label Text="Qualifications" Style="{StaticResource HeaderLabel}" />
                    <Grid Padding="6" ColumnDefinitions="*, Auto" ColumnSpacing="10">
                        <Label Text="Name" VerticalOptions="Center" TextColor="Gray" FontAttributes="Bold"/>
                        <Label Text="Level" VerticalOptions="Center" HorizontalOptions="End" TextColor="Gray" FontAttributes="Bold"/>
                    </Grid>

                    <CollectionView ItemsSource="{Binding Qualifications, Converter={StaticResource QualificationSorter}}" SelectionMode="None">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="local:Qualification">
                                <Grid Padding="6" ColumnDefinitions="*,Auto,Auto" ColumnSpacing="10">
                                    <Label Text="{Binding ., Converter={StaticResource QualificationNameConverter}}" VerticalOptions="Center" HorizontalOptions="Start" />
                                    <Button Grid.Column="1" Text="{Binding ., Converter={StaticResource LiteralTranslationConverter}, ConverterParameter='Choose'}" FontSize="12" Padding="10,2"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type vm:CharacterViewModel}}, Path=SelectQualificationAttributeCommand}"
                                            CommandParameter="{Binding .}">
                                        <Button.IsVisible>
                                            <MultiBinding Converter="{StaticResource QualificationChooseButtonVisibleConverter}">
                                                <Binding Path="NeedsSelection" />
                                                <Binding Path="IsSelectable" />
                                                <Binding Source="{RelativeSource AncestorType={x:Type vm:CharacterViewModel}}" Path="CanReviseQualificationSelection" />
                                            </MultiBinding>
                                        </Button.IsVisible>
                                    </Button>
                                    <Label Grid.Column="2" Text="{Binding QualificationLevel, Converter={StaticResource TranslationConverter}}" VerticalOptions="Center" HorizontalTextAlignment="End" />
                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>
            </Border>

            <!-- Percent qualifications -->
            <Border Style="{StaticResource BoxBorder}" Margin="0,10">
                <VerticalStackLayout>
                    <Label Text="Percent qualifications" Style="{StaticResource HeaderLabel}" />
                    <mc:EntryWithLabel Label="PP" Text="{Binding PercentQualificationPoints}" IsReadOnly="True" />
                    <Grid Padding="6" ColumnDefinitions="*, Auto, Auto" ColumnSpacing="10">
                        <Label Text="Name" VerticalOptions="Center" TextColor="Gray" FontAttributes="Bold"/>
                        <Label Text="%" Grid.Column="1" VerticalOptions="Center" HorizontalOptions="End" TextColor="Gray" FontAttributes="Bold"/>
                    </Grid>

                    <CollectionView ItemsSource="{Binding PercentQualifications, Converter={StaticResource QualificationSorter}}" SelectionMode="None">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="local:PercentQualification">
                                <Grid Padding="6" ColumnDefinitions="*,Auto,Auto" ColumnSpacing="10">
                                    <Label Text="{Binding Name, Converter={StaticResource TranslationConverter}}" VerticalOptions="Center" HorizontalOptions="Start" />
                                    <Label Text="{Binding Percent}" Grid.Column="1" VerticalOptions="Center" HorizontalTextAlignment="End" />
                                    <Button Grid.Column="2" Text="+3%" FontSize="12" Padding="10,2"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type vm:CharacterViewModel}}, Path=IncreasePercentQualificationCommand}"
                                            CommandParameter="{Binding .}"
                                            IsVisible="{Binding Source={RelativeSource AncestorType={x:Type vm:CharacterViewModel}}, Path=CanAllocatePercentQualificationPoints}" />
                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>
            </Border>

            <!-- Special Abilities -->
            <Border Style="{StaticResource BoxBorder}" Margin="0,10">
                <VerticalStackLayout>
                    <Label Text="Special Abilities" Style="{StaticResource HeaderLabel}" />
                    <Grid Padding="6" ColumnDefinitions="*" ColumnSpacing="10">
                        <Label Text="Name" VerticalOptions="Center" TextColor="Gray" FontAttributes="Bold"/>
                    </Grid>

                    <CollectionView ItemsSource="{Binding SpecialQualifications, Converter={StaticResource QualificationSorter}}" SelectionMode="None">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="q:ISpecialQualification">
                                <Grid Padding="6" ColumnDefinitions="*" ColumnSpacing="10">
                                    <Label Text="{Binding ., Converter={StaticResource SpecialQualificationNameConverter}}" VerticalOptions="Center" HorizontalOptions="Start" />
                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>
            </Border>

        </VerticalStackLayout>
    </ScrollView>
</ContentView>
