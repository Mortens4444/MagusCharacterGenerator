<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MAGUS.Assistant.ViewModels"
             xmlns:mc="clr-namespace:Mtf.Maui.Controls;assembly=Mtf.Maui.Controls"
             xmlns:conv="clr-namespace:Mtf.LanguageService.MAUI.Converters;assembly=Mtf.LanguageService.MAUI"
             xmlns:mcconv="clr-namespace:Mtf.Maui.Controls.Converters;assembly=Mtf.Maui.Controls"
             x:Class="MAGUS.Assistant.Views.CharacterOverviewView"
             x:ClassModifier="internal"
             x:DataType="vm:CharacterViewModel">

    <ContentView.Resources>
        <ResourceDictionary>
            <conv:EnumDescriptionTranslationConverter x:Key="EnumDescriptionTranslationConverter" />
            <conv:TranslationConverter x:Key="TranslationConverter" />
            <mcconv:InverseBooleanConverter x:Key="InverseBooleanConverter" />
        </ResourceDictionary>
    </ContentView.Resources>

    <ScrollView>
        <VerticalStackLayout Spacing="16" Padding="16">

            <!-- Hunger and sleep: always visible, turn red once critical (see the Care tab for Sleep/Eat/Cast actions) -->
            <!--<Border Style="{StaticResource BoxBorder}">
                <VerticalStackLayout Spacing="4">
                    <Label Text="{Binding HungerPercent, StringFormat='🍗 Hunger: {0:F0}%'}" FontAttributes="Bold">
                        <Label.Triggers>
                            <DataTrigger TargetType="Label" Binding="{Binding IsHungerCritical}" Value="True">
                                <Setter Property="TextColor" Value="#FF3B30" />
                            </DataTrigger>
                        </Label.Triggers>
                    </Label>
                    <Label Text="{Binding SleepPercent, StringFormat='😴 Rest: {0:F0}%'}" FontAttributes="Bold">
                        <Label.Triggers>
                            <DataTrigger TargetType="Label" Binding="{Binding IsSleepCritical}" Value="True">
                                <Setter Property="TextColor" Value="#FF3B30" />
                            </DataTrigger>
                        </Label.Triggers>
                    </Label>
                </VerticalStackLayout>
            </Border>-->

            <!-- Portrait -->
            <Border Style="{StaticResource BoxBorder}">
                <HorizontalStackLayout Spacing="12">
                    <Image Source="{Binding PortraitImage}" HeightRequest="80" WidthRequest="80" Aspect="AspectFill" />
                    <Button Text="Change portrait" Command="{Binding ChangePortraitCommand}" VerticalOptions="Center" />
                </HorizontalStackLayout>
            </Border>

            <!-- Name, race, class, alinment, experience level -->
            <Border Style="{StaticResource BoxBorder}">
                <VerticalStackLayout>
                    <Grid ColumnDefinitions="*,Auto">
                        <mc:EntryWithLabel Label="Character name" Text="{Binding Character.Name}" />
                        <Button Grid.Column="1" Text="🖨️" Command="{Binding PrintCommand}" VerticalOptions="End" Margin="10,0,0,5" HeightRequest="40"/>
                    </Grid>

                    <BoxView HeightRequest="1" Color="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}" Margin="0,5"/>
                    <Grid ColumnDefinitions="*,*" RowDefinitions="Auto, Auto, Auto, Auto, Auto, Auto" MinimumWidthRequest="100" RowSpacing="5">

                        <mc:EntryWithLabel Label="Race"
                                           Text="{Binding Race, Converter={StaticResource TranslationConverter}}"
                                           HorizontalOptions="Center"
                                           IsReadOnly="True"
                                           Grid.Column="0" />

                        <mc:EntryWithLabel Label="Caste"
                                           Text="{Binding Class, Converter={StaticResource TranslationConverter}}"
                                           HorizontalOptions="Center"
                                           IsReadOnly="True"
                                           Grid.Column="1" />

                        <VerticalStackLayout Grid.Row="1">
                            <Label Text="Alignment" HorizontalOptions="Center" />
                            <Picker ItemsSource="{Binding Alignments}"
                                    ItemDisplayBinding="{Binding ., Converter={StaticResource EnumDescriptionTranslationConverter}}"
                                    SelectedItem="{Binding Alignment}"
                                    VerticalTextAlignment="Center"
                                    HorizontalTextAlignment="Center"
                                    MaximumHeightRequest="50" />
                        </VerticalStackLayout>

                        <VerticalStackLayout Grid.Row="1" Grid.Column="1">
                            <Label Text="Deity" HorizontalOptions="Center" />
                            <Picker ItemsSource="{Binding Deities}"
                                ItemDisplayBinding="{Binding ., Converter={StaticResource EnumDescriptionTranslationConverter}}"
                                SelectedItem="{Binding Deity}"
                                VerticalTextAlignment="Center"
                                HorizontalTextAlignment="Center"
                                MaximumHeightRequest="50" />
                        </VerticalStackLayout>

                        <mc:EntryWithLabel Label="Experience level"
                                           Text="{Binding Level}"
                                           HorizontalOptions="Center"
                                           IsReadOnly="True"
                                           Grid.Row="2" />

                        <mc:EntryWithLabel Label="Experience points"
                                           Text="{Binding ExperiencePoints}"
                                           HorizontalOptions="Center"
                                           IsReadOnly="True"
                                           Grid.Row="2"
                                           Grid.Column="1" />
                        
                        <Button Text="Level Up"
                                Command="{Binding LevelUpCommand}"
                                IsVisible="{Binding CanLevelUp}"
                                Grid.Row="3"
                                Grid.ColumnSpan="2"
                                HorizontalOptions="Center" />

                        <!--<Grid Grid.Row="1" ColumnDefinitions="*, Auto">
                            <Label Grid.Column="0" Text="Player character" VerticalOptions="Center" LineBreakMode="WordWrap" />
                            <Switch Grid.Column="1" IsToggled="{Binding PlayerCharacter}" />
                        </Grid>-->

                    </Grid>
                </VerticalStackLayout>
            </Border>

        </VerticalStackLayout>
    </ScrollView>
</ContentView>