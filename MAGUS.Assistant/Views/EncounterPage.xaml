<?xml version="1.0" encoding="utf-8" ?>
<v:NotifierPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                xmlns:mc="clr-namespace:Mtf.Maui.Controls;assembly=Mtf.Maui.Controls"
                xmlns:m="clr-namespace:MAGUS.Assistant.Models"
                xmlns:v="clr-namespace:Mtf.LanguageService.MAUI.Views;assembly=Mtf.LanguageService.MAUI"                
                xmlns:vm="clr-namespace:MAGUS.Assistant.ViewModels"
                xmlns:mv="clr-namespace:MAGUS.Assistant.Views"
                xmlns:b="clr-namespace:MAGUS.Bestiary;assembly=MAGUS"
                xmlns:gs="clr-namespace:MAGUS.GameSystem;assembly=MAGUS"
                xmlns:conv="clr-namespace:Mtf.LanguageService.MAUI.Converters;assembly=Mtf.LanguageService.MAUI"
                xmlns:c="clr-namespace:MAGUS.Assistant.Converters"
                xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
                x:Class="MAGUS.Assistant.Views.EncounterPage"
                x:ClassModifier="internal"
                x:DataType="vm:EncounterViewModel"
                Title="Clash">

    <v:NotifierPage.Resources>
        <ResourceDictionary>
            <c:BoolToTextConverter x:Key="BoolToTextConverter" />
            <c:AttackModeNameConverter x:Key="AttackModeNameConverter" />
            <conv:TranslationConverter x:Key="TranslationConverter" />
        </ResourceDictionary>
    </v:NotifierPage.Resources>

    <!-- RowDefinitions módosítva: top:Auto, assignments:Auto, turns:*, bottom:Auto -->
    <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto,*,Auto" Padding="12">

        <!-- Characters -->
        <VerticalStackLayout Spacing="8" Margin="0,0,0,15" IsVisible="{Binding ShowControls}">
            <Label Text="Characters" FontAttributes="Bold" FontSize="18" />

            <Picker ItemsSource="{Binding AvailableCharacters}"
                    SelectedItem="{Binding SelectedCharacter, Mode=TwoWay}"
                    ItemDisplayBinding="{Binding Name}"
                    Title="Select character" />

            <Button Text="&#x2B07;" Command="{Binding AddCharacterCommand}" />

            <Button Text="🛡 Protect" Command="{Binding AddProtectedAllyCommand}"
                    ToolTipProperties.Text="Add an ally to fight alongside the selected assignment's character, for a protect-in-combat quest they've accepted" />
        </VerticalStackLayout>

        <!-- Enemies -->
        <VerticalStackLayout Grid.Column="1" Spacing="8" Margin="0,0,0,15" IsVisible="{Binding ShowControls}">
            <Label Text="Enemies" FontAttributes="Bold" FontSize="18"/>

            <Grid ColumnDefinitions="*, Auto" ColumnSpacing="5">
                <Picker Grid.Column="0"
                        ItemsSource="{Binding AvailableEnemies}"
                        SelectedItem="{Binding SelectedEnemy}"
                        ItemDisplayBinding="{Binding Name, Converter={StaticResource TranslationConverter}}"
                        Title="Select enemy" />

                <Button Grid.Column="1" Text="🎲" FontSize="18" Padding="10,0"
                        Command="{Binding PickRandomEnemyCommand}"
                        ToolTipProperties.Text="Pick random enemy" />
            </Grid>

            <Button Text="&#x2B07;" Command="{Binding AddEnemyCommand}"/>
        </VerticalStackLayout>

        <!-- Assignments -->
        <VerticalStackLayout Grid.Row="1" Grid.ColumnSpan="2" Spacing="8" Margin="0,0,0,15" IsVisible="{Binding ShowControls}">
            <Label Text="Pairing" FontAttributes="Bold" FontSize="18" />

            <CollectionView ItemsSource="{Binding Assignments}"
                            SelectedItem="{Binding SelectedAssignment}"
                            SelectionMode="Single">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="vm:AssignmentViewModel">
                        <Border Padding="8" Margin="4">
                            <StackLayout Spacing="6">

                                <HorizontalStackLayout Spacing="8">
                                    <Image Source="{Binding Character.RandomImage}" HeightRequest="48" WidthRequest="48" Aspect="AspectFill" />
                                    <Label Text="{Binding Character.Name}" FontAttributes="Bold" VerticalOptions="Center" />
                                </HorizontalStackLayout>
                                <!--<Label Text="{Binding Character.Speed, StringFormat='Speed: {0} m/s'}" FontSize="12" />-->

                                <Button Text="{Binding SelectedAttackMode, Converter={StaticResource AttackModeNameConverter}, StringFormat='Attack mode: {0}'}"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:EncounterViewModel}}, Path=SelectAttackModeCommand}"
                                        CommandParameter="{Binding .}" />

                                <CollectionView ItemsSource="{Binding Enemies}" MaximumHeightRequest="140">
                                    <CollectionView.ItemTemplate>
                                        <!-- No x:DataType here on purpose: an enemy can be a Creature or a Character
                                             (see EncounterViewModel.LoadBestiaryAsync) - both expose RandomImage/Name,
                                             but not through a shared Attacker-level member, so this binds by
                                             reflection against whichever concrete type each item actually is. -->
                                        <DataTemplate>
                                            <Grid ColumnDefinitions="Auto,*,Auto" Padding="0,4">
                                                <Image Grid.Column="0" Source="{Binding RandomImage}" HeightRequest="36" WidthRequest="36" Aspect="AspectFill" />
                                                <Label Grid.Column="1" Text="{Binding Name, Converter={StaticResource TranslationConverter}}" Margin="8,0,0,0" VerticalOptions="Center" />
                                                <!--<Button MaximumHeightRequest="10" MaximumWidthRequest="10" Text="X" />--> <!--Need Delete Enemy from assignment function with nicer delte buttons, nice text UTF-8 delete char-->
                                                <!--<Label Grid.Column="1" Text="{Binding Distance, StringFormat='{0} m'}" />-->
                                                <!--<Label Grid.Column="1" Text="{Binding Status}" />-->
                                            </Grid>
                                        </DataTemplate>
                                    </CollectionView.ItemTemplate>
                                </CollectionView>

                                <!--<Label Text="{Binding EstimatedTime, StringFormat='ETA: {0:F1} s'}" FontSize="12" TextColor="Gray" />-->
                            </StackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>

        <!-- Turns details (most now a stretchable row) -->
        <Grid Grid.Row="2" Grid.ColumnSpan="2" RowDefinitions="Auto, *">
            <Border Grid.Row="0" Padding="8" BackgroundColor="{AppThemeBinding Light=#F0F0F0, Dark=#333333}">
                <Grid Padding="6" ColumnDefinitions="5*,5*,3*,7*" RowDefinitions="Auto,Auto">
                    <Label Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Text="Attacker" FontAttributes="Bold" VerticalOptions="Center" />
                    <Label Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Text="Attack" HorizontalTextAlignment="End" FontAttributes="Bold" />
                    <Label Grid.Row="1" Grid.Column="1" Text="Result" FontAttributes="Bold" />
                    <Label Grid.Row="1" Grid.Column="2" Text="Direction" FontAttributes="Bold" />
                    <Label Grid.Row="1" Grid.Column="3" Text="Location" FontAttributes="Bold" />
                </Grid>
            </Border>

            <CollectionView Grid.Row="1"
                            ItemsSource="{Binding SelectedTurnHistory}"
                            x:Name="TurnHistoryView"
                            VerticalOptions="Fill">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="vm:TurnViewModel">
                        <VerticalStackLayout Spacing="6">
                            <HorizontalStackLayout BackgroundColor="{StaticResource Tertiary}">
                                <Label Text="{Binding Source='Round', Converter={StaticResource TranslationConverter}}" FontAttributes="Bold" Padding="10,5" />
                                <Label Text="{Binding Turn.Round}" FontAttributes="Bold" Padding="10,5" />
                            </HorizontalStackLayout>

                            <Border Padding="8" Margin="4">
                                <CollectionView ItemsSource="{Binding Attacks}">
                                    <CollectionView.ItemTemplate>
                                        <DataTemplate x:DataType="vm:TurnAttackViewModel">
                                            <Grid Padding="6" ColumnDefinitions="5*,5*,3*,7*">
                                                <Image Grid.Column="0" Source="{Binding AttackerImage}" HeightRequest="32" WidthRequest="32" Aspect="AspectFill" HorizontalOptions="Start" />
                                                <Label Grid.Column="1" Text="{Binding Result}" />
                                                <Label Grid.Column="2" Text="{Binding Direction}" />
                                                <Label Grid.Column="3" Text="{Binding HitLocation}" />
                                            </Grid>
                                        </DataTemplate>
                                    </CollectionView.ItemTemplate>
                                </CollectionView>
                            </Border>
                        </VerticalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>

        <!-- Run turn + toggle (docked to bottom, scrolls sideways so no button ever gets clipped on narrow screens) -->
        <ScrollView Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal">
            <HorizontalStackLayout Spacing="8">
                <Button Command="{Binding ToggleShowControlsCommand}"
                        Text="{Binding ShowControls, Converter={StaticResource BoolToTextConverter}, ConverterParameter='Hide pairing|Show pairing'}"/>
                <Button Text="Psi surge" Command="{Binding UsePsiSurgeCommand}" IsVisible="{Binding SelectedCharacterHasPsi}" />
                <Button Text="Dismantle shield" Command="{Binding DismantlePsiShieldCommand}" IsVisible="{Binding SelectedCharacterHasPsi}" />
                <Button Text="Empower spell" Command="{Binding EmpowerSpellCommand}" IsVisible="{Binding SelectedCharacterHasSorcery}" />
                <Button Text="Start New Round" Command="{Binding RunTurnCommand}" FontAttributes="Bold" />
            </HorizontalStackLayout>
        </ScrollView>

    </Grid>

</v:NotifierPage>