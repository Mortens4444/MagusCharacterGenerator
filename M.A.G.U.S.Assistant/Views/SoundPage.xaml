<?xml version="1.0" encoding="utf-8" ?>
<v:NotifierPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:v="clr-namespace:M.A.G.U.S.Assistant.Views"
             xmlns:vm="clr-namespace:M.A.G.U.S.Assistant.ViewModels"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:mc="clr-namespace:Mtf.Maui.Controls;assembly=Mtf.Maui.Controls"
             x:Class="M.A.G.U.S.Assistant.Views.SoundPage"
             Title="Sounds">

    <v:NotifierPage.BindingContext>
        <vm:SoundViewModel />
    </v:NotifierPage.BindingContext>

    <Grid Padding="12" RowDefinitions="Auto,*,Auto" RowSpacing="8">

        <mc:EntryWithLabel Label="Search"
                       Text="{Binding SearchText}"
                       Grid.Row="0" />

        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding FilteredSounds}"
                        SelectionMode="Single"
                        SelectedItem="{Binding SelectedSound}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="8">
                        <Label Text="{Binding DisplayName}" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <StackLayout Grid.Row="2" Orientation="Horizontal" Spacing="10" HorizontalOptions="Center">
            <Button Text="Play" Command="{Binding PlayCommand}" IsEnabled="{Binding CanPlay}" />
            <Button Text="Stop" Command="{Binding StopCommand}" IsEnabled="{Binding IsPlaying}" />
            <Label Text="Vol" VerticalOptions="Center" />
            <Slider Minimum="0" Maximum="1" Value="{Binding Volume}" WidthRequest="140" />
        </StackLayout>
    </Grid>
</v:NotifierPage>
